# 教师个人博客 — 使用指南

## 一、访问地址

### 前台页面（公开访问）

| 页面 | 地址 | 说明 |
|------|------|------|
| 首页 | http://localhost:5173 | 轮播图 + 个人介绍 + 最新文章 |
| 文章列表 | http://localhost:5173/articles | 分页浏览所有已发布文章 |
| 文章详情 | http://localhost:5173/articles/:id | 查看单篇文章内容 |
| 留言板 | http://localhost:5173/messages | 查看留言 + 提交留言 |

### 后台管理（需登录）

| 页面 | 地址 | 说明 |
|------|------|------|
| 登录页 | http://localhost:5173/admin/login | 管理员登录入口 |
| 仪表盘 | http://localhost:5173/admin | 数据统计 + 快捷入口 |
| 个人介绍管理 | http://localhost:5173/admin/profile | 富文本编辑个人介绍 |
| 轮播图管理 | http://localhost:5173/admin/carousel | 上传/删除/排序轮播图 |
| 文章管理 | http://localhost:5173/admin/articles | 文章列表 + 增删改查 |
| 写新文章 | http://localhost:5173/admin/articles/edit | 富文本编辑器写文章 |
| 编辑文章 | http://localhost:5173/admin/articles/edit/:id | 编辑已有文章 |
| 留言审核 | http://localhost:5173/admin/messages | 审核/删除/排序留言 |

---

## 二、管理员账号

| 项目 | 值 |
|------|-----|
| 用户名 | `admin` |
| 密码 | `admin123456` |

> 如需修改账号密码，编辑 `backend/.env` 文件中的 `ADMIN_USERNAME` 和 `ADMIN_PASSWORD`，然后删除 `backend/data/teacherblog.db` 重新初始化。

---

## 三、本地开发启动步骤

### 1. 启动后端

```bash
cd backend
npm install          # 安装依赖（首次）
npm run init-db      # 初始化数据库（首次）
npm run dev          # 启动开发服务
```

后端运行在 http://localhost:3001

### 2. 启动前端

```bash
cd frontend
npm install          # 安装依赖（首次）
npm run dev          # 启动开发服务
```

前端运行在 http://localhost:5173，已配置代理自动转发 `/api` 请求到后端。

### 3. 访问页面

浏览器打开 http://localhost:5173 即可看到前台首页。

后台管理打开 http://localhost:5173/admin/login，输入上面的账号密码登录。

---

## 四、各功能使用说明

### 4.1 首页轮播图

1. 登录后台，进入「轮播图管理」
2. 点击「上传轮播图」按钮，选择图片（支持 jpg/png/gif/webp，最大 10MB）
3. 可为每张轮播图填写标题（可选）
4. 拖拽列表项可调整轮播顺序，排序自动保存
5. 前台首页顶部会自动展示轮播图

> 如果未上传任何轮播图，首页会显示一个占位区域提示前往上传。

### 4.2 个人介绍

1. 登录后台，进入「个人介绍管理」
2. 在富文本编辑器中编辑内容（支持文字、图片、格式等）
3. 编辑器内粘贴图片或使用工具栏上传图片
4. 点击「保存」按钮

### 4.3 文章管理

1. 登录后台，进入「文章管理」
2. 点击「写新文章」
3. 填写标题、摘要（可选）、上传封面图（可选）
4. 在富文本编辑器中撰写正文
5. 选择状态：草稿 / 发布
6. 点击「保存」
7. 在文章列表中可编辑或删除已有文章

> 只有状态为「发布」的文章才会在前台展示。草稿不会出现在公开页面。

### 4.4 留言功能

**访客留言（前台）：**
1. 打开 http://localhost:5173/messages
2. 填写昵称和留言内容
3. 点击「提交留言」
4. 提交后提示「留言已提交，等待管理员审核」

**管理员审核（后台）：**
1. 登录后台，进入「留言审核」
2. 在「待审核」标签下查看新留言
3. 点击「通过」或「拒绝」
4. 通过的留言会显示在前台留言板
5. 可在「已通过」标签下拖拽调整留言展示顺序
6. 点击「保存排序」按钮保存

---

## 五、技术信息

### 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| 前端开发服务 | 5173 | Vite dev server |
| 后端 API 服务 | 3001 | Express |
| 前端 API 代理 | /api → :3001 | Vite 自动代理 |

### 项目目录结构

```
teacherblog/
├── frontend/                 # 前端项目（Vue 3 + Vite + Element Plus）
│   ├── src/
│   │   ├── api/              # API 请求封装
│   │   ├── assets/           # 全局样式
│   │   ├── layouts/          # 布局组件（前台 + 后台）
│   │   ├── router/           # 路由配置
│   │   ├── stores/           # Pinia 状态管理
│   │   └── views/            # 页面组件
│   │       ├── Home.vue
│   │       ├── ArticleList.vue
│   │       ├── ArticleDetail.vue
│   │       ├── Messages.vue
│   │       └── admin/        # 后台管理页面
│   │           ├── Login.vue
│   │           ├── Dashboard.vue
│   │           ├── ProfileManage.vue
│   │           ├── CarouselManage.vue
│   │           ├── ArticleManage.vue
│   │           ├── ArticleEdit.vue
│   │           └── MessageManage.vue
│   ├── vite.config.js
│   └── package.json
│
├── backend/                  # 后端项目（Node.js + Express + sql.js）
│   ├── src/
│   │   ├── app.js            # Express 入口
│   │   ├── db/
│   │   │   ├── index.js      # 数据库初始化（建表 + 默认数据）
│   │   │   └── init.js       # 初始化脚本
│   │   ├── middleware/
│   │   │   ├── auth.js       # JWT 认证
│   │   │   ├── db-save.js    # 数据库自动持久化
│   │   │   └── error-handler.js
│   │   └── routes/
│   │       ├── auth.js       # 登录
│   │       ├── profile.js    # 个人介绍
│   │       ├── carousel.js   # 轮播图
│   │       ├── articles.js   # 文章
│   │       ├── messages.js   # 留言
│   │       └── upload.js     # 图片上传
│   ├── uploads/              # 上传的图片文件
│   ├── data/                 # SQLite 数据库文件
│   │   └── teacherblog.db
│   ├── .env                  # 环境变量配置
│   └── package.json
│
├── nginx/
│   └── teacherblog.conf      # Nginx 部署配置
├── 技术方案.md
├── 使用指南.md                # 本文件
└── README.md
```

### API 接口一览

#### 公开接口（无需认证）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/profile | 获取个人介绍 |
| GET | /api/carousel | 获取轮播图列表 |
| GET | /api/articles?page=1&pageSize=12 | 获取已发布文章列表 |
| GET | /api/articles/:id | 获取文章详情 |
| GET | /api/messages | 获取已审核通过的留言 |
| POST | /api/messages | 提交新留言 |

#### 管理接口（需 JWT 认证）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/login | 管理员登录，返回 JWT |
| PUT | /api/admin/profile | 更新个人介绍 |
| GET | /api/admin/carousel | 获取所有轮播图 |
| POST | /api/admin/carousel | 新增轮播图 |
| PUT | /api/admin/carousel/:id | 编辑轮播图 |
| DELETE | /api/admin/carousel/:id | 删除轮播图 |
| PUT | /api/admin/carousel/sort | 调整轮播图顺序 |
| GET | /api/admin/articles | 获取所有文章（含草稿） |
| GET | /api/admin/articles/:id | 获取单篇文章 |
| POST | /api/admin/articles | 新建文章 |
| PUT | /api/admin/articles/:id | 编辑文章 |
| DELETE | /api/admin/articles/:id | 删除文章 |
| GET | /api/admin/messages | 获取所有留言（含未审核） |
| PUT | /api/admin/messages/:id/review | 审核留言 |
| DELETE | /api/admin/messages/:id | 删除留言 |
| PUT | /api/admin/messages/sort | 调整留言顺序 |
| POST | /api/admin/upload | 上传图片 |

### 环境变量配置

后端环境变量文件位于 `backend/.env`：

| 变量名 | 默认值 | 说明 |
|--------|--------|------|
| PORT | 3001 | 后端服务端口 |
| JWT_SECRET | teacherblog-secret-key | JWT 签名密钥 |
| ADMIN_USERNAME | admin | 默认管理员用户名 |
| ADMIN_PASSWORD | admin123456 | 默认管理员密码 |
| UPLOAD_DIR | uploads | 上传文件目录 |

---

## 六、生产部署

### 构建前端

```bash
cd frontend
npm run build    # 产物输出到 frontend/dist/
```

### 启动后端（生产模式）

```bash
cd backend
npm start        # node src/app.js
```

建议使用 PM2 守护进程：

```bash
npm install -g pm2
pm2 start src/app.js --name teacherblog-api
pm2 save
pm2 startup      # 设置开机自启
```

### Nginx 配置

参考 `nginx/teacherblog.conf`，核心配置：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        root /var/www/teacherblog/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3001;
    }

    location /uploads/ {
        alias /path/to/teacherblog/backend/uploads/;
    }
}
```

---

## 七、常见问题

### Q: 忘记管理员密码怎么办？
A: 修改 `backend/.env` 中的 `ADMIN_PASSWORD`，删除 `backend/data/teacherblog.db`，重新运行 `npm run init-db`。

### Q: 上传的图片存储在哪里？
A: 存储在 `backend/uploads/` 目录，通过 `http://localhost:3001/uploads/文件名` 访问。

### Q: 数据库文件在哪里？
A: `backend/data/teacherblog.db`，备份时复制此文件即可。

### Q: 前端页面空白怎么办？
A: 确认后端服务已启动（端口 3001），前端开发服务已启动（端口 5173），浏览器控制台查看是否有报错。

### Q: 生产环境如何更新代码？
A: 前端重新 `npm run build` 替换 dist 目录；后端用 `pm2 restart teacherblog-api` 重启。
