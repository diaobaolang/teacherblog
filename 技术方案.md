# 教师个人博客 — 技术方案

## 一、整体架构

**前后端分离**，前端和后端部署在同一台服务器上。

```
┌─────────────────────────────────────────────────┐
│                   同一台服务器                    │
│                                                   │
│  ┌─────────────┐     ┌──────────────────────┐   │
│  │  Nginx      │     │  后端 API 服务         │   │
│  │  (80/443)   │     │  Node.js + Express    │   │
│  │             │────▶│  (端口 3001)           │   │
│  │  静态前端   │     │                        │   │
│  │  反向代理API │     │  ┌──────────────────┐ │   │
│  │             │     │  │  SQLite 数据库    │ │   │
│  └─────────────┘     │  │  (文件级,免运维)  │ │   │
│                      │  └──────────────────┘ │   │
│                      └──────────────────────┘   │
└─────────────────────────────────────────────────┘
```

## 二、技术选型

| 层级 | 技术 | 理由 |
|------|------|------|
| **前端** | Vue 3 + Vite | 生态成熟，构建快，适合个人项目规模 |
| **UI 框架** | Element Plus | 组件丰富，自带响应式，PC/移动端兼容好 |
| **后端** | Node.js + Express | 与前端同语言，部署简单，单进程即可 |
| **数据库** | SQLite (better-sqlite3) | 文件级数据库，零配置，单服务器场景最优 |
| **后台认证** | JWT | 轻量，无需 session 存储 |
| **图片存储** | 本地文件系统 | 通过 Nginx 或 Express 静态服务访问 |
| **Web 服务器** | Nginx | 托管前端静态文件 + 反向代理后端 API |

## 三、功能模块设计

### 1. 个人介绍页（首页）

- **文字介绍**：富文本编辑（使用 wangeditor 或类似编辑器），后台可编辑
- **轮播图**：后台可上传/删除/排序图片，前端自动轮播展示
- 数据结构：
  - `profile` 表：介绍文字内容
  - `carousel_images` 表：`id, image_url, sort_order, title, created_at`

### 2. 文章模块

- **文章列表页**：分页展示，支持按时间倒序
- **文章详情页**：富文本内容渲染
- 后台管理：新增/编辑/删除文章，支持富文本编辑（含图片上传）
- 数据结构：
  - `articles` 表：`id, title, content, cover_image, summary, status(draft/published), sort_order, created_at, updated_at`

### 3. 留言模块

- **前端提交留言**：访客填写昵称 + 内容（无需登录）
- **后台审核**：管理员审核通过/拒绝/删除
- **排序可调**：后台可拖拽调整留言展示顺序
- **展示逻辑**：仅展示审核通过的留言，按配置的顺序排列
- 数据结构：
  - `messages` 表：`id, nickname, content, status(pending/approved/rejected), sort_order, created_at, reviewed_at`

## 四、后台管理系统

- 统一的管理后台路由 `/admin`
- 登录页（JWT 认证）
- Dashboard：概览
- 个人介绍管理：编辑文字 + 轮播图管理
- 文章管理：CRUD + 富文本编辑
- 留言审核：审核 + 排序

## 五、数据库表结构总览

```sql
-- 管理员账号
CREATE TABLE users (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    username    TEXT NOT NULL UNIQUE,
    password_hash TEXT NOT NULL,
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 个人介绍内容
CREATE TABLE profile (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    content     TEXT,
    updated_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 轮播图
CREATE TABLE carousel_images (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    image_url   TEXT NOT NULL,
    title       TEXT,
    sort_order  INTEGER DEFAULT 0,
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 文章
CREATE TABLE articles (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    title       TEXT NOT NULL,
    content     TEXT,
    cover_image TEXT,
    summary     TEXT,
    status      TEXT DEFAULT 'draft',  -- draft / published
    sort_order  INTEGER DEFAULT 0,
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 留言
CREATE TABLE messages (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    nickname    TEXT NOT NULL,
    content     TEXT NOT NULL,
    status      TEXT DEFAULT 'pending',  -- pending / approved / rejected
    sort_order  INTEGER DEFAULT 0,
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
    reviewed_at DATETIME
);
```

## 六、API 路由设计

```
# 公开 API
GET    /api/profile                          获取个人介绍
GET    /api/carousel                         获取轮播图列表
GET    /api/articles                         文章列表（分页）
GET    /api/articles/:id                     文章详情
POST   /api/messages                         提交留言

# 管理 API（需 JWT 认证）
POST   /api/auth/login                       管理员登录

PUT    /api/admin/profile                    更新个人介绍
GET    /api/admin/carousel                   轮播图列表
POST   /api/admin/carousel                   上传轮播图
PUT    /api/admin/carousel/:id               编辑轮播图
DELETE /api/admin/carousel/:id               删除轮播图
PUT    /api/admin/carousel/sort              调整轮播图顺序

GET    /api/admin/articles                   文章列表
POST   /api/admin/articles                   新建文章
PUT    /api/admin/articles/:id              编辑文章
DELETE /api/admin/articles/:id              删除文章

GET    /api/admin/messages                   留言列表（含未审核）
PUT    /api/admin/messages/:id/review        审核留言
DELETE /api/admin/messages/:id              删除留言
PUT    /api/admin/messages/sort              调整留言顺序

POST   /api/admin/upload                     通用图片上传
```

## 七、项目目录结构

```
teacherblog/
├── frontend/              # 前端项目
│   ├── src/
│   │   ├── views/         # 页面 (Home, ArticleList, ArticleDetail, Message, Admin)
│   │   ├── components/    # 组件 (Carousel, Navbar, ArticleCard, ...)
│   │   ├── router/        # 路由
│   │   ├── api/           # axios 请求封装
│   │   ├── stores/        # Pinia 状态管理
│   │   └── assets/        # 静态资源
│   └── vite.config.ts
│
├── backend/               # 后端项目
│   ├── src/
│   │   ├── routes/        # 路由 (auth, profile, articles, messages, carousel, upload)
│   │   ├── middleware/     # 中间件 (auth, error-handler)
│   │   ├── db/            # 数据库初始化 + 迁移
│   │   ├── services/      # 业务逻辑
│   │   └── app.js         # Express 应用入口
│   ├── uploads/           # 上传文件存储目录
│   └── package.json
│
├── data/                  # SQLite 数据库文件
│   └── teacherblog.db
│
├── nginx/                 # Nginx 配置
│   └── teacherblog.conf
│
└── README.md
```

## 八、部署方案（同一台服务器）

### Nginx 配置

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /var/www/teacherblog/frontend/dist;
        try_files $uri $uri/ /index.html;   # SPA history 模式
    }

    # API 反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:3001;
    }

    # 上传文件访问
    location /uploads/ {
        alias /path/to/backend/uploads/;
    }
}
```

### 后端进程守护

```bash
# 使用 PM2 守护后端进程
pm2 start src/app.js --name teacherblog-api
pm2 save
pm2 startup   # 开机自启
```

## 九、响应式设计策略

- 使用 CSS Flexbox + Grid 布局
- Element Plus 的响应式栅格系统（`<el-row :gutter="20">`）
- 导航栏在移动端折叠为汉堡菜单
- 文章列表在 PC 端双栏/三栏，移动端单栏
- 通过 CSS 媒体断点适配：`@media (max-width: 768px)`

## 十、总结

| 维度 | 方案 |
|------|------|
| 前端 | Vue 3 + Vite + Element Plus + Pinia |
| 后端 | Node.js + Express + better-sqlite3 |
| 认证 | JWT |
| 部署 | Nginx 托管前端 + 反向代理后端 + PM2 守护 |
| 数据库 | SQLite（零运维，适合单服务器） |
| 响应式 | CSS 媒体查询 + Element Plus 响应式组件 |

这个方案的优势：**技术栈统一（全 JS）、部署简单（无需 MySQL）、维护成本低、适合个人博客规模**。
